package com.quizgame.auth.config;

/**
 * File này đã bị vô hiệu hóa hoàn toàn để tránh xung đột CORS
 * Cấu hình CORS được đặt trong SecurityConfig.java
 */
public class WebConfig {
    // Class rỗng để tránh lỗi biên dịch
}
